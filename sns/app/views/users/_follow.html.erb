<%= form_for(current_user.active_relationships.build,
  html: { method: :post }, url: user_followings_path(@user)) do |f| %>
<%= hidden_field_tag :followee_id, @user.id %>
<%= f.submit "Follow" %>
<% end %>
