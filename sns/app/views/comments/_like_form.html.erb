<% if comment.like_user(current_user) %>
   <%= render 'comments/unlike', :comment_like => comment.like_user(current_user) %>
<% else %>
     <%= render 'comments/like', :comment_like => comment.comment_likes.build %>
<% end %>
