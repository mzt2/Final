<% if comment.liked_by(current_user) %>
   <%= render 'comments/unlike', :comment_like => comment.liked_by(current_user) %>
<% else %>
     <%= render 'comments/like', :comment_like => comment.comment_likes.build %>
<% end %>
