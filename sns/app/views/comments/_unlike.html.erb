<%= form_for(comment_like,
  html: { method: :delete },
  url: comment_like_path(comment_id: comment_like.comment_id, id: current_user.id)) do |f| %>
<%= hidden_field_tag :post_id, params[:id] %>
<%= f.submit "", class: "unlike" %>
<% end %>
