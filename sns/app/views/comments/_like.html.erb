<%= form_for(comment_like,
  html: { method: :post }, url: comment_comment_likes_path(comment_like.comment_id)) do |f| %>
<%= hidden_field_tag :user_id, current_user.id %>
<%= hidden_field_tag :post_id, params[:id] %>
<%= f.submit "", class: "like" %>
<% end %>
